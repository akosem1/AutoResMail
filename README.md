# AutoResMail
A script to set up a simple automated batch for emails in an organization.


To run, you must log in via powershell - normal!

Navigate to the file location:
cd "c:\AutoResMail"

Run the file:
.\AutoResMail.ps1

It will then go to the identification screen
